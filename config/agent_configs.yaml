# Agent Configurations

researcher:
  name: "Research Agent"
  model: "grok-beta"
  temperature: 0.3
  max_tokens: 2000
  tools:
    - web_search
    - document_retrieval

writer:
  name: "Writer Agent"
  model: "grok-beta"
  temperature: 0.7
  max_tokens: 3000
  tools:
    - grammar_check

reviewer:
  name: "Reviewer Agent"
  model: "grok-beta"
  temperature: 0.2
  max_tokens: 2000
